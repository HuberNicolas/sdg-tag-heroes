# Portainer creates its admin account in the web UI on first start (http://localhost:1000).
# docker-compose.yml requires this file; the values are not read by Portainer itself.
ADMIN_USERNAME=
ADMIN_PASSWORD=
