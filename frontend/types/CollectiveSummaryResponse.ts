export interface CollectiveSummaryResponse {
  summary: string; // A single sentence that captures the overarching themes of the publications
  keywords: string[]; // An array of keywords representing the nature of the publications
}
